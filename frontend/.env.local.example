# Backend API URL used by Next.js rewrites (next.config.mjs)
# All /api/* requests are proxied to this URL, avoiding CORS.
# NEXT_PUBLIC_API_URL is exposed to the client bundle.
# BACKEND_URL is server-side only and takes priority if set.
NEXT_PUBLIC_API_URL=http://localhost:8084
# BACKEND_URL=http://localhost:8084

# Enable bundle analyzer (opens treemap in browser after build)
# ANALYZE=true
