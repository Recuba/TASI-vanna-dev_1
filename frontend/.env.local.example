# =============================================================================
# Ra'd AI Frontend - Environment Variables
# =============================================================================
# Copy this file to .env.local and configure values.
#
# Legend:
#   [REQUIRED]  - Must be set for the app to function
#   [optional]  - Has a sensible default; override if needed
#
# Variables prefixed with NEXT_PUBLIC_ are exposed to the client bundle.
# Variables without the prefix are server-side only.

# ---------------------------------------------------------------------------
# API Connection
# ---------------------------------------------------------------------------
# [REQUIRED] Backend API URL. All /api/* requests are proxied to this URL
# via Next.js rewrites (next.config.mjs), avoiding CORS issues.
NEXT_PUBLIC_API_URL=http://localhost:8084

# [optional] Server-side only backend URL. Takes priority over NEXT_PUBLIC_API_URL
# if set. Use this when the server-side URL differs from the client-side URL
# (e.g. internal Docker network vs public URL).
# BACKEND_URL=http://localhost:8084

# ---------------------------------------------------------------------------
# Security
# ---------------------------------------------------------------------------
# [optional] Comma-separated allowed hosts for host validation middleware.
# In production, set to your deployment domain(s).
# Default: localhost,localhost:3000,raid-ai-app-production.up.railway.app
NEXT_PUBLIC_ALLOWED_HOSTS=localhost,localhost:3000

# [optional] CSP violation report URI. Set to receive Content-Security-Policy
# violation reports. Leave empty to disable reporting.
# NEXT_PUBLIC_CSP_REPORT_URI=

# ---------------------------------------------------------------------------
# Authentication
# ---------------------------------------------------------------------------
# [optional] Session timeout in minutes. After this period of inactivity,
# the user is redirected to the login page.
# Default: 30
NEXT_PUBLIC_SESSION_TIMEOUT=30

# [optional] Redirect URL for unauthenticated users trying to access
# protected routes.
# Default: /login
NEXT_PUBLIC_AUTH_REDIRECT=/login

# ---------------------------------------------------------------------------
# Monitoring & Error Tracking
# ---------------------------------------------------------------------------
# [optional] Sentry DSN for error tracking and performance monitoring.
# Get your DSN from https://sentry.io/
# Leave empty to disable Sentry.
# NEXT_PUBLIC_SENTRY_DSN=

# [optional] Enable client-side monitoring (Web Vitals, custom metrics).
# Default: false
NEXT_PUBLIC_ENABLE_MONITORING=false

# ---------------------------------------------------------------------------
# Build & Development
# ---------------------------------------------------------------------------
# [optional] Enable Next.js bundle analyzer. Opens a treemap in the browser
# after build showing bundle composition.
# ANALYZE=true
