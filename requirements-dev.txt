# Development and testing dependencies
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

# Testing
pytest>=8.0.0,<9.0
pytest-asyncio>=0.24.0,<1.0
pytest-cov>=5.0.0,<6.0
pytest-timeout>=2.3.0,<3.0
httpx>=0.27.0,<1.0

# Ingestion (optional runtime dep, required for xbrl_processor tests)
openpyxl>=3.1.0,<4.0

# Load testing
locust>=2.20.0,<3.0
